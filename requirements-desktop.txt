# Runtime + packaging deps for the SuperNova AI Windows desktop build.
# NOTE: this is the SLIM set used for the standalone .exe (mock inference).
# It deliberately omits torch/opencv/segmentation-models so the build stays
# small and reliable. To ship the real models, use run_desktop.bat instead,
# which installs the full backend/requirements.txt (including torch).

# --- backend runtime ---
fastapi==0.115.6
greenlet==3.0.3
uvicorn[standard]==0.32.1
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
Pillow==11.0.0
numpy==2.0.2
python-multipart==0.0.20
reportlab==4.2.5

# --- desktop window + packaging ---
pywebview==5.3.2
pyinstaller==6.11.1
